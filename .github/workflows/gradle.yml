name: Hello World Workflow

on:
  push: 
    branches: 
      - main
  pull request:
    branches:
      - main
workflow_dispatch:

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: hello world
        run: echo "Hello Sakshi"
        shell: bash

  HelooooAgain
    runs-on: ubuntu-latest
    steps:
      - name: Hello again the world
        run: echo "Hello again to the world"
        shell: bash
